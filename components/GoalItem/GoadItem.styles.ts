import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    marginVertical: 5,
    paddingVertical: 15,
    paddingHorizontal: 10,
    fontSize: 20,
    borderColor: '#ddd',
    borderWidth: 1,
  },
});
