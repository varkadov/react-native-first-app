import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    paddingBottom: 30,
    paddingLeft: 30,
    paddingRight: 30,
  },
  listItem: {
    marginVertical: 5,
    paddingVertical: 15,
    paddingHorizontal: 10,
    fontSize: 20,
    borderColor: '#ddd',
    borderWidth: 1,
  },
});
